# Drop portrait files here for homepage featured quote cards.
# Filenames must match `photo:` in data/featured-quotes-2026.yaml (any common image extension).
# Examples: joe-neguse.jpg, judy-amabile.png, michael-dougherty.jpg, ashley-stolzmann.jpg
# Omit `photo:` in the YAML for anyone without a portrait.
#
# Preferred photo size:
#   600 × 600 px (square), JPG, under ~150 KB
#   Face centered — flush left square strip (~240px), does not stretch with quote height
#   Preferred: 600 × 600 px JPG


#   400 × 400 is fine; no need larger than ~900 × 900
